# ============================================================================
# Docker Compose for Easypanel Deployment
# ============================================================================

version: "3.8"

services:
  hermes-admin:
    image: ghcr.io/m4rrec0s/oraculo/hermes-enterprise:latest
    environment:
      # --- Profile (hardcoded) ---
      ENTERPRISE_PROFILE: admin
      AGENT_NAME: ${ADMIN_AGENT_NAME:-Hermes}
      # --- Model ---
      MODEL_PROVIDER: ${ADMIN_MODEL_PROVIDER:-nvidia}
      MODEL_NAME: ${ADMIN_MODEL_NAME:-nvidia/nemotron-3-super-120b-a12b}
      MODEL_BASE_URL: ${ADMIN_MODEL_BASE_URL:-https://integrate.api.nvidia.com/v1}
      API_KEY_VAR: NVIDIA_API_KEY
      NVIDIA_API_KEY: ${NVIDIA_API_KEY}
      OPENAI_API_KEY: ""
      # --- API server ---
      API_SERVER_ENABLED: "true"
      API_SERVER_HOST: 0.0.0.0
      API_SERVER_PORT: "8081"
      API_SERVER_KEY: ${API_SERVER_KEY}
      API_SERVER_CORS_ORIGINS: "*"
      # --- Dashboard ---
      DASHBOARD_ENABLED: "true"
      DASHBOARD_PORT: "9119"
      DASHBOARD_HOST: 0.0.0.0
      DASHBOARD_USERNAME: ${DASHBOARD_USERNAME:-admin}
      DASHBOARD_PASSWORD: ${DASHBOARD_PASSWORD:-}
      # --- Database: URL (parse no entrypoint) ---
      DATABASE_URL: ${DATABASE_URL:-}
      # --- Database: vars convencionais (fallback / direto) ---
      HERMES_PG_HOST: ${HERMES_PG_HOST:-}
      HERMES_PG_PORT: ${HERMES_PG_PORT:-5432}
      HERMES_PG_DATABASE: ${HERMES_PG_DATABASE:-hermes}
      HERMES_PG_USER: ${HERMES_PG_USER:-}
      HERMES_PG_PASSWORD: ${HERMES_PG_PASSWORD:-}
      HERMES_PG_SSL: ${HERMES_PG_SSL:-false}
      # --- Redis: URL (parse no entrypoint) ---
      REDIS_URL: ${REDIS_URL:-}
      # --- Redis: vars convencionais (fallback / direto) ---
      HERMES_REDIS_HOST: ${HERMES_REDIS_HOST:-}
      HERMES_REDIS_PORT: ${HERMES_REDIS_PORT:-6379}
      HERMES_REDIS_PASSWORD: ${HERMES_REDIS_PASSWORD:-}
      # --- Cesto APIs ---
      CESTO_API_URL: ${CESTO_API_URL:-}
      CESTO_API_KEY: ${CESTO_API_KEY:-}
      CESTO_API_EMAIL: ${CESTO_API_EMAIL:-}
      CESTO_API_PASSWORD: ${CESTO_API_PASSWORD:-}
      CESTO_PROD_PG_HOST: ${CESTO_PROD_PG_HOST:-}
      CESTO_PROD_PG_PORT: ${CESTO_PROD_PG_PORT:-54320}
      CESTO_PROD_PG_DATABASE: ${CESTO_PROD_PG_DATABASE:-}
      CESTO_PROD_PG_USER: ${CESTO_PROD_PG_USER:-}
      CESTO_PROD_PG_PASSWORD: ${CESTO_PROD_PG_PASSWORD:-}
      CESTO_PG_HOST: ${CESTO_PG_HOST:-}
      CESTO_PG_PORT: ${CESTO_PG_PORT:-54320}
      CESTO_PG_DATABASE: ${CESTO_PG_DATABASE:-}
      CESTO_PG_USER: ${CESTO_PG_USER:-}
      CESTO_PG_PASSWORD: ${CESTO_PG_PASSWORD:-}
      # --- WaHa ---
      WAHA_API_URL: ${WAHA_API_URL:-}
      WAHA_API_KEY: ${WAHA_API_KEY:-}
      WAHA_INSTANCE: ${WAHA_INSTANCE:-}
      WHATSAPP_GROUP_ID: ${WHATSAPP_GROUP_ID:-}
      # --- Telegram ---
      TELEGRAM_BOT_TOKEN: ${TELEGRAM_BOT_TOKEN:-}
      # --- Misc ---
      HERMES_LOG_LEVEL: ${HERMES_LOG_LEVEL:-INFO}
      HERMES_TUI: "false"
      HERMES_HEADLESS: "true"
      GATEWAY_ALLOW_ALL_USERS: ${GATEWAY_ALLOW_ALL_USERS:-false}
      HOME: /home/hermes
    working_dir: /home/hermes
    volumes:
      - hermes-admin-data:/home/hermes/.hermes
    expose:
      - "8081"
      - "9119"
    restart: unless-stopped
    networks:
      - hermes-enterprise-network

  hermes-atendimento:
    image: ghcr.io/m4rrec0s/oraculo/hermes-enterprise:latest
    environment:
      # --- Profile (hardcoded) ---
      ENTERPRISE_PROFILE: atendimento
      SKIP_ANA_SCHEMA: "true"
      AGENT_NAME: Ana
      # --- Model ---
      MODEL_PROVIDER: openai
      MODEL_NAME: ${ANA_MODEL_NAME:-gpt-5-nano}
      MODEL_BASE_URL: ${ANA_MODEL_BASE_URL:-https://api.openai.com/v1}
      API_KEY_VAR: OPENAI_API_KEY
      OPENAI_API_KEY: ${OPENAI_API_KEY}
      NVIDIA_API_KEY: ""
      # --- API server ---
      API_SERVER_ENABLED: "true"
      API_SERVER_HOST: 0.0.0.0
      API_SERVER_PORT: "8082"
      API_SERVER_KEY: ${API_SERVER_KEY}
      API_SERVER_CORS_ORIGINS: "*"
      # --- Dashboard desabilitado ---
      DASHBOARD_ENABLED: "false"
      # --- Database: URL ---
      DATABASE_URL: ${DATABASE_URL:-}
      # --- Database: vars convencionais ---
      HERMES_PG_HOST: ${HERMES_PG_HOST:-}
      HERMES_PG_PORT: ${HERMES_PG_PORT:-5432}
      HERMES_PG_DATABASE: ${HERMES_PG_DATABASE:-hermes}
      HERMES_PG_USER: ${HERMES_PG_USER:-}
      HERMES_PG_PASSWORD: ${HERMES_PG_PASSWORD:-}
      HERMES_PG_SSL: ${HERMES_PG_SSL:-false}
      # --- Redis: URL ---
      REDIS_URL: ${REDIS_URL:-}
      # --- Redis: vars convencionais ---
      HERMES_REDIS_HOST: ${HERMES_REDIS_HOST:-}
      HERMES_REDIS_PORT: ${HERMES_REDIS_PORT:-6379}
      HERMES_REDIS_PASSWORD: ${HERMES_REDIS_PASSWORD:-}
      # --- Cesto APIs ---
      CESTO_API_URL: ${CESTO_API_URL:-}
      CESTO_API_KEY: ${CESTO_API_KEY:-}
      CESTO_API_EMAIL: ${CESTO_API_EMAIL:-}
      CESTO_API_PASSWORD: ${CESTO_API_PASSWORD:-}
      CESTO_PROD_PG_HOST: ${CESTO_PROD_PG_HOST:-}
      CESTO_PROD_PG_PORT: ${CESTO_PROD_PG_PORT:-54320}
      CESTO_PROD_PG_DATABASE: ${CESTO_PROD_PG_DATABASE:-}
      CESTO_PROD_PG_USER: ${CESTO_PROD_PG_USER:-}
      CESTO_PROD_PG_PASSWORD: ${CESTO_PROD_PG_PASSWORD:-}
      CESTO_PG_HOST: ${CESTO_PG_HOST:-}
      CESTO_PG_PORT: ${CESTO_PG_PORT:-54320}
      CESTO_PG_DATABASE: ${CESTO_PG_DATABASE:-}
      CESTO_PG_USER: ${CESTO_PG_USER:-}
      CESTO_PG_PASSWORD: ${CESTO_PG_PASSWORD:-}
      # --- WaHa ---
      WAHA_API_URL: ${WAHA_API_URL:-}
      WAHA_API_KEY: ${WAHA_API_KEY:-}
      WAHA_INSTANCE: ${WAHA_INSTANCE:-}
      WHATSAPP_GROUP_ID: ${WHATSAPP_GROUP_ID:-}
      # --- Telegram ---
      TELEGRAM_BOT_TOKEN: ${TELEGRAM_BOT_TOKEN:-}
      # --- Misc ---
      HERMES_LOG_LEVEL: ${HERMES_LOG_LEVEL:-INFO}
      HERMES_TUI: "false"
      HERMES_HEADLESS: "true"
      GATEWAY_ALLOW_ALL_USERS: ${GATEWAY_ALLOW_ALL_USERS:-false}
      HOME: /home/hermes
    working_dir: /home/hermes
    volumes:
      - hermes-atendimento-data:/home/hermes/.hermes
    expose:
      - "8082"
    restart: unless-stopped
    networks:
      - hermes-enterprise-network

volumes:
  hermes-admin-data:
  hermes-atendimento-data:

networks:
  hermes-enterprise-network:
    driver: bridge
